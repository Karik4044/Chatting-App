<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

  <display-name>Chat Application</display-name>

  <welcome-file-list>
    <welcome-file>chat.jsp</welcome-file>
  </welcome-file-list>

  <!-- TCP Server Listener -->
  <listener>
    <listener-class>com.example.chat.TCP.Server.TCPServerManager</listener-class>
  </listener>

  <!-- Servlet mapping cho ChatServlet -->
  <servlet>
    <servlet-name>Clientservlet</servlet-name>
    <servlet-class>com.example.chat.Servlet.Clientservlet</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>Clientservlet</servlet-name>
    <url-pattern>/chat</url-pattern>
  </servlet-mapping>

  <!-- Cấu hình encoding cho request -->
  <filter>
    <filter-name>CharacterEncodingFilter</filter-name>
    <filter-class>org.apache.catalina.filters.SetCharacterEncodingFilter</filter-class>
    <init-param>
      <param-name>encoding</param-name>
      <param-value>UTF-8</param-value>
    </init-param>
    <init-param>
      <param-name>ignore</param-name>
      <param-value>false</param-value>
    </init-param>
  </filter>

  <filter-mapping>
    <filter-name>CharacterEncodingFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <!-- Cấu hình session timeout (30 phút) -->
  <session-config>
    <session-timeout>30</session-timeout>
  </session-config>

  <!-- Error pages -->
  <error-page>
    <error-code>404</error-code>
    <location>/error.jsp</location>
  </error-page>

  <error-page>
    <error-code>500</error-code>
    <location>/error.jsp</location>
  </error-page>

</web-app>